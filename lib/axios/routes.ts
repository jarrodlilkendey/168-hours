export enum routes {
    mobsters = 'mobsters',
    character = 'character',
    user = 'user',
    users = 'users',
    // shows = "shows",
    // bands = "bands",
    // reservations = "reservations",
    // images = "images",
}
